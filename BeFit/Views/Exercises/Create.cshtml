@model BeFit.Models.Exercise

@{
    ViewData["Title"] = "Create Exercise";
}

<div class="container mt-5">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="fw-bold text-primary">Create Exercise</h1>
        <a asp-action="Index" class="btn btn-secondary btn-lg">
            &larr; Back to List
        </a>
    </div>

    <div class="card shadow-lg border-0 rounded-4">
        <div class="card-body p-4">

            <h4 class="mb-4">Exercise</h4>

            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <div class="mb-3">
                    <label asp-for="DateTimeBeginning" class="form-label"></label>
                    <input asp-for="DateTimeBeginning" class="form-control" type="datetime-local" />
                    <span asp-validation-for="DateTimeBeginning" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="DateTimeEnding" class="form-label"></label>
                    <input asp-for="DateTimeEnding" class="form-control" type="datetime-local" />
                    <span asp-validation-for="DateTimeEnding" class="text-danger"></span>
                </div>

                <button type="submit" class="btn btn-primary btn-lg">Create</button>
            </form>

        </div>
    </div>

</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
